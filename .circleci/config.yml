version: 2.1

# orbs:
#   python: cimg/python:3.7.12

jobs:
  build-docker-image: 
    machine: true
    steps:
      - checkout
      - run:
          name: Build docker image
          command: | 
            docker build .
            #docker push https://quay.io/repository/aofarrel/orfalyzer:circleci
  

workflows: 
  main: 
    jobs: 
      - build-docker-image
